[package]
name = "dsn-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive", "env"] }
config = { version = "0.15", features = ["toml", "yaml", "json", "ini"] }
